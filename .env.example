# Why: Copy this file to `.env` and replace the user/password details; Git ignores `.env` so credentials stay local.
DATABASE_URL=postgresql+psycopg://your_postgres_user:replace_with_your_password@localhost:5432/south_florida_hospital_quality
