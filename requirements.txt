# Why: pandas cleans and joins the CMS tabular extracts without manual row-by-row processing.
pandas>=2.2,<3.0
# Why: matplotlib writes portable chart images for review and later Power BI design decisions.
matplotlib>=3.8,<4.0
# Why: seaborn keeps statistical chart styling consistent and readable.
seaborn>=0.13,<1.0
# Why: SQLAlchemy provides a consistent PostgreSQL connection layer for pandas data loads.
SQLAlchemy>=2.0,<3.0
# Why: psycopg is the modern PostgreSQL driver used by SQLAlchemy to load this model.
psycopg[binary]>=3.2,<4.0
# Why: python-dotenv reads a local DATABASE_URL from .env without exposing credentials in Git.
python-dotenv>=1.0,<2.0
